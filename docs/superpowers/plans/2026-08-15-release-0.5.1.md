# 0.5.1 Release Prep Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bump `@pi-vault/pi-tools` from `0.5.0` to `0.5.1`, move the TinyFish provider entry out of `[0.5.0]` and into `[0.5.1]`, add the architecture-deepening refactor notes, and update the README callout.

**Architecture:** Documentation-only release. Three files change: `package.json` (version), `CHANGELOG.md` (TinyFish relay + new release section), `README.md` (callout). No source code, no tests, no new dependencies.

**Tech Stack:** Markdown, JSON, `pnpm` (verification only).

---

## File Structure

- **Modify** `package.json` — single-line version bump.
- **Modify** `CHANGELOG.md` — remove TinyFish from `[0.5.0]`, replace `[Unreleased]` with `## [0.5.1] - 2026-08-15`, add Unreleased-comparison link.
- **Modify** `README.md` — replace `## What's new in 0.5.0` callout with `## What's new in 0.5.1` callout.

No new files. No source code touched.

---

## Task 1: Bump version in `package.json`

**Files:**
- Modify: `package.json:3`

- [ ] **Step 1: Edit the version field**

Locate the line `"version": "0.5.0"` in `package.json` (line 3) and change it to `"version": "0.5.1"`.

- [ ] **Step 2: Verify the change**

Run: `grep -n '"version"' package.json`
Expected: `3:  "version": "0.5.1",`

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "chore: bump version to 0.5.1"
```

---

## Task 2: Update `CHANGELOG.md`

**Files:**
- Modify: `CHANGELOG.md` (top section plus bottom comparison link)

- [ ] **Step 1: Remove TinyFish from `[0.5.0]`**

In `CHANGELOG.md`, the `[0.5.0]` section does **not** currently list TinyFish (per the spec — TinyFish was added after 0.5.0). Re-confirm by running:

Run: `grep -n -i tinyfish CHANGELOG.md`
Expected: Exactly one match, on a line inside `[Unreleased]`.

If a TinyFish line is found inside `[0.5.0]` (defensive check), delete that single line. Otherwise skip this step.

- [ ] **Step 2: Replace `[Unreleased]` with the new `0.5.1` section**

Edit the top of `CHANGELOG.md` so the section header block reads exactly:

```markdown
## [Unreleased]

## [0.5.1] - 2026-08-15

### Added

- TinyFish Search and Fetch provider support through `TINYFISH_API_KEY`.

### Changed

- Search filter support is now explicit and strictly enforced; `web_search` honors abort signals during filter resolution.
- Provider catalog is centralized so registration order and metadata stay consistent across the registry.
- Provider operation policy is unified behind a single execution hook shared by all providers.
- Config refresh is aligned with the tool lifecycle; reload payloads are validated to prevent config corruption.
- Content extraction has a fallback seam with simplified error handling and hardened cancellation propagation.
- Updated Biome schema to `2.5.8`.
```

Keep the `## [Unreleased]` header in place (empty, ready for the next release).

- [ ] **Step 3: Add the Unreleased-comparison link at the bottom**

The current file ends at the `[0.1.0]` section. Append a new comparison link at the very end of the file. Locate the last existing link reference (e.g. `[0.1.0]: https://github.com/pi-vault/pi-tools/releases/tag/v0.1.0`) and add a new line directly after it:

```markdown
[0.5.1]: https://github.com/pi-vault/pi-tools/releases/tag/v0.5.1
```

- [ ] **Step 4: Verify the change**

Run: `grep -n -E '^## \[|^### ' CHANGELOG.md`
Expected output (relevant lines):

```
7:## [Unreleased]
9:## [0.5.1] - 2026-08-15
11:### Added
15:### Changed
23:## [0.5.0] - 2026-07-21
```

Plus existing comparison link `0.5.1` at the bottom.

- [ ] **Step 5: Commit**

```bash
git add CHANGELOG.md
git commit -m "docs: move TinyFish to 0.5.1 and add architecture-deepening notes"
```

---

## Task 3: Update `README.md` callout

**Files:**
- Modify: `README.md` (lines 12–19, the `## What's new in 0.5.0` block)

- [ ] **Step 1: Replace the callout header**

In `README.md`, change the line `## What's new in 0.5.0` to `## What's new in 0.5.1`.

- [ ] **Step 2: Replace the callout body**

Replace the entire blockquote body (lines 13–19, the four-bullet block starting with `> **Dashboard replaces typed`) with exactly:

```markdown
> **TinyFish provider added.** `TINYFISH_API_KEY` enables keyless search and fetch through the new TinyFish provider — see the [provider table](#available-providers) for setup.
>
> **Internal architecture deepened.** Search filter enforcement, provider catalog, operation policy, runtime lifecycle, and content extraction now share clearer seams. No public API changes.
```

- [ ] **Step 3: Verify the change**

Run: `grep -n -E '^## What.s new' README.md`
Expected: `13:## What's new in 0.5.1`

Run: `awk '/## What.s new in 0.5.1/,/^## Install/' README.md | head -20`
Expected: The new callout block precedes the `## Install` section, with the two bullets above.

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "docs: refresh README callout for 0.5.1"
```

---

## Task 4: Run release verification

**Files:** none (read-only).

- [ ] **Step 1: Run `pnpm check`**

Run: `pnpm check`
Expected: Biome lint, `tsc --noEmit`, and `vitest run` all pass with exit code 0. No source files were touched, so this should pass quickly.

If any check fails, **stop** and investigate — the release prep should not have broken anything.

- [ ] **Step 2: Run `pnpm release:check`**

Run: `pnpm release:check`
Expected: `pnpm check` plus `pnpm pack --dry-run` both pass. The dry-run should include the same files as `0.5.0` (`src`, `docs/assets`, `LICENSE`, `CHANGELOG.md`, `README.md`).

- [ ] **Step 3: Verify the final diff**

Run: `git log --oneline -5`
Expected: Three new commits on top of the spec commit (d1940e8) — `chore: bump version to 0.5.1`, `docs: move TinyFish to 0.5.1 and add architecture-deepening notes`, `docs: refresh README callout for 0.5.1`.

Run: `git status`
Expected: `nothing to commit, working tree clean`.

Run: `git diff --stat HEAD~3 HEAD`
Expected:

```
 CHANGELOG.md | 18 ++++++++++++++++++
 README.md    | 14 +++++++-------
 package.json |  2 +-
 3 files changed, 22 insertions(+), 12 deletions(-)
```

(Exact line counts may vary by one or two if the TinyFish line removal step was skipped.)

- [ ] **Step 4: Stop. Do not tag, do not push.**

Per the user request, no `git tag` and no `git push`. The release is ready for the user to publish manually.
